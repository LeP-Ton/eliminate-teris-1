# Touch Bar 方块列数从 12 调整为 16

## 背景与目标
- 用户要求：Touch Bar 当前显示 12 个方块，改为显示 16 个。

## 约束与原则
- 不改动玩法规则与交互逻辑，仅调整列数配置。
- 继续使用现有 `columns` 单点配置驱动控制器与 Touch Bar 视图。

## 阶段与 TODO
- [x] 将列数常量从 12 调整为 16。
- [x] 更新项目认知与索引文档。
- [x] 完成构建验证。

## 当前进展
- Touch Bar 列数已改为 16，显示范围同步扩展。

## 代码变更
- Sources/GameViewController.swift
```diff
diff --git a/Sources/GameViewController.swift b/Sources/GameViewController.swift
index b73fa25..fbd8a26 100644
--- a/Sources/GameViewController.swift
+++ b/Sources/GameViewController.swift
@@ -40,7 +40,7 @@ final class GameViewController: NSViewController, NSTouchBarDelegate {
-    private let columns = 12
+    private let columns = 16
```

- AGENTS.md
```diff
diff --git a/AGENTS.md b/AGENTS.md
index 81911ee..b9fd95c 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -20,3 +20,4 @@
 - 各模块（游戏设置/时间与得分/挑战记录/玩法说明）标题前已加入主题色图标，并统一保留约 6px 的图标与标题间距。
 - 玩法说明标题图标已切换为兼容性更高的 `doc.text`，并增加系统符号缺失时的回退图标，避免个别系统版本不显示。
 - 模式切换时会锁定窗口 frame，避免自由/竞分/竞速切换引发窗口尺寸跳变；并在切换后强制按最终布局重算挑战记录两端对齐文本。
+- Touch Bar 方块列数已从 12 扩展到 16，控制区可同时显示 16 个可交互方块。
```

- .agentdocs/index.md
```diff
diff --git a/.agentdocs/index.md b/.agentdocs/index.md
@@
 ## 当前变更文档
+`workflow/20260221134551-touchbar-columns-16.md` - Touch Bar 方块列数从 12 调整为 16。
@@
 ## 读取场景
+- 需要确认“Touch Bar 显示 16 个方块”时，优先读取 `20260221134551` 文档。
@@
 ## 关键记忆
+- Touch Bar 当前列数为 16（原 12），`GameViewController` 通过 `columns` 常量驱动 `GameBoardController` 与 `GameTouchBarView` 的显示范围。
```

## 测试用例
### TC-001 Touch Bar 列数显示
- 类型：UI测试
- 操作步骤：启动应用并观察 Touch Bar 方块区。
- 预期结果：可见 16 个方块槽位/交互列。

### TC-002 构建验证
- 类型：构建测试
- 操作步骤：执行 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift build`。
- 预期结果：构建成功。
